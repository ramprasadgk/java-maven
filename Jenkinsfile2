@Library('test') _

pipeline {
    
agent any
 stages {
         stage('stage 0'){
             steps{
                 script{
                    echo sh(returnStdout: true, script: 'env')
                    
                    setGitHubPullRequestStatus state: 'SUCCESS', context: 'custom-context1', message: "Run "
                    gitHubPRStatus statusMessage: message('run')
                    echo 'done'
      
                 }
             }
         }
     stage('first'){
         steps{
             script {
                logs.info 'Starting'
                logs.warning 'Nothing to do!'
                sayHello()
                sayHello
                sayHello('sdf')
        
                
             }
         }
     }
 }

}
